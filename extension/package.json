{
  "name": "sidechats-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:browser": "node build.mjs && node scripts/browser-check.mjs",
    "check:live": "node build.mjs && node scripts/live-check.mjs",
    "check:invalidation": "node build.mjs && node scripts/context-invalidation-check.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "playwright": "^1.62.1",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
